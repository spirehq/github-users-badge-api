if Template.subscriptionsReady
	.panel.panel-success
		.panel-heading
			h3.panel-title
				| There are {{usages}} mentions of
				a.repository-link(href=repositoryUrl) {{path}}
		.panel-body
				ol
				each file in files
					li
						a(href=file.url) {{file.name}}
				else

			if filesLoading
				+loader
			else
				if noFiles
					span No mentions were found
				else if hasMoreFiles
					div(id="showMoreResults")
